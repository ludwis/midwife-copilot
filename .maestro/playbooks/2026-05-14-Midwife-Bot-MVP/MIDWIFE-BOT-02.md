# Midwife Bot — Phase 2: Co-pilot Core (Backend)

Reference plan: `midwife-assistant-bot-mvp-v4.md` in the project root.
Depends on Phase 1 being complete (project scaffold, config, ingestion, audit modules).

Project root: `/Users/ad4m/Library/Mobile Documents/com~apple~CloudDocs/Projects/stilla-app`
All modules are importable from project root (confirmed by `tests/conftest.py` from Phase 1).

---

- [ ] Create `bot/safety.py`. It must classify an inbound message on a 4-level urgency scale: `LOW`, `NORMAL`, `HIGH`, `URGENT`. Use Vertex AI Gemini 2.0 Flash with a system prompt that instructs it to classify pregnancy/birth/postpartum messages for urgency. The prompt must explicitly handle oblique descriptions (not just obvious keywords). Return a `SafetyResult` dataclass: `{"level": str, "reason": str}`. Expose `classify(message: str, project: str, location: str) -> SafetyResult`. Abstract the Gemini API call behind `_call_gemini(prompt: str) -> str` for mocking. Write `tests/bot/test_safety.py` with at least 5 test cases using `pytest-mock`: bleeding + pain → `URGENT`, "is this normal" question → `NORMAL`, headache + vision changes → `HIGH`, normal Braxton Hicks question → `NORMAL`, model returns malformed JSON → falls back to `HIGH` (fail-closed). Run `pytest tests/bot/test_safety.py` and confirm all pass.

- [ ] Create `bot/retriever.py`. It must query the Vertex AI Search **production** data store and return the top-N relevant knowledge chunks. Expose `retrieve(query: str, datastore_id: str, project: str, location: str, top_n: int = 5) -> list[dict]` where each dict has `{"chunk_id": str, "question": str, "answer": str, "score": float}`. Use the `google-cloud-discoveryengine` `SearchServiceClient`. Abstract the client call behind `_search(query: str, ...) -> list` for mocking. Write `tests/bot/test_retriever.py` with at least 3 test cases using `pytest-mock`: successful retrieval returns ranked chunks, empty results returns empty list, API error raises `RuntimeError` with descriptive message. Run `pytest tests/bot/test_retriever.py` and confirm all pass.

- [ ] Create `bot/generator.py`. It must take a client message plus a list of retrieved knowledge chunks and generate a grounded draft reply using Vertex AI Gemini 2.0 Flash. The system prompt must instruct the model to: answer ONLY from the provided reference material, cite chunk IDs used, write in a warm but professional midwifery tone, and explicitly say "I don't have enough information" if retrieved chunks don't cover the question. Return a `DraftResult` dataclass: `{"text": str, "cited_chunk_ids": list[str], "confidence": float}`. Expose `generate_draft(message: str, chunks: list[dict], project: str, location: str) -> DraftResult`. Abstract the Gemini call behind `_call_gemini(prompt: str) -> str` for mocking. Write `tests/bot/test_generator.py` with at least 4 test cases using `pytest-mock`: normal generation returns text and citations, empty chunks returns low-confidence "insufficient info" draft, model returns malformed JSON → raises `ValueError`, very long message is truncated to context limit. Run `pytest tests/bot/test_generator.py` and confirm all pass.

- [ ] Create `bot/conversation.py`. It must manage per-conversation state in Firestore. Define a `ConversationState` enum: `PENDING_INVITE`, `AWAITING_CONSENT`, `ACTIVE`, `WINDOW_CLOSED`, `PAUSED`, `HUMAN_ONLY`, `ARCHIVED`. Expose these functions (all async): `create_invite(client_id: str, token: str, db) -> dict`, `get_by_token(token: str, db) -> dict | None`, `get_by_phone(phone: str, db) -> dict | None`, `transition_state(conversation_id: str, new_state: ConversationState, db) -> None`, `record_inbound(conversation_id: str, message: str, db) -> None`, `record_draft(conversation_id: str, draft: dict, db) -> None`, `record_outbound(conversation_id: str, text: str, source: str, db) -> None`, `update_window_timestamp(conversation_id: str, db) -> None`, `is_window_open(conversation_id: str, db) -> bool` (checks `last_client_inbound_at` + 24h). Abstract Firestore calls so the `db` parameter can be a mock. Write `tests/bot/test_conversation.py` with at least 8 test cases using `pytest-mock`: create invite sets correct initial state, get_by_token returns None for unknown token, state transition persists to Firestore, record_inbound updates timestamp, is_window_open returns True within 24h, is_window_open returns False after 24h, invalid state transition raises `ValueError`, record_outbound stores source tag. Run `pytest tests/bot/test_conversation.py` and confirm all pass.

- [ ] Create `api/main.py` — the FastAPI application entry point. It must: instantiate the FastAPI app with title "Midwife Bot API", mount routers (stubs for now: `webhook`, `ask`, `admin` from `api/routes/`), serve the Vue PWA static files from `frontend/dist/` as a catch-all mount (using `StaticFiles` with `html=True`), and configure CORS for the PWA origin in dev. Create stub router files `api/routes/webhook.py`, `api/routes/ask.py`, `api/routes/admin.py` that each define the router but return `{"status": "not implemented"}` for all routes (so they can be registered without error). Write `tests/api/test_main.py` using `httpx.AsyncClient` with `ASGITransport`: assert the app starts without error, GET `/health` returns 200 with `{"status": "ok"}` (add this route to `main.py`), GET `/api/ask` returns 405 (method not allowed — POST only). Run `pytest tests/api/test_main.py` and confirm all pass.

- [ ] Implement `api/routes/ask.py` — the `POST /ask` endpoint. It must: require `Authorization: Bearer {ADMIN_TOKEN}` header (return 401 otherwise), accept body `{"user_id": str, "message": str, "conversation_id": str | None}`, call `safety.classify()`, call `retriever.retrieve()`, call `generator.generate_draft()`, call `audit.log_event("draft_generated", ...)`, return `{"draft": str, "urgency": str, "sources": list[str], "requires_review": true}`. `requires_review` is always `true` in v4. Inject `Settings` via FastAPI dependency. Write `tests/api/test_ask.py` using `httpx.AsyncClient` with all downstream functions mocked: successful request returns correct schema with `requires_review: true`, missing auth header returns 401, `safety.classify()` returning HIGH still returns a draft (only the PWA UI suppresses the draft for HIGH — the endpoint always generates), invalid JSON body returns 422. Run `pytest tests/api/test_ask.py` and confirm all pass.

- [ ] Hook `bot/audit.py` into the full inbound message flow: update `api/routes/ask.py` to call `audit.log_event("inbound", ...)` at the start of request handling (before safety check) and `audit.log_event("draft_generated", ...)` after generation (or `audit.log_event("inbound", ..., extra={"urgency": "HIGH"})` when triage result is HIGH and no draft is generated). Write `tests/api/test_ask_audit.py` that mocks `audit.log_event` and asserts it is called exactly twice per successful request (once for inbound, once for draft), and once per HIGH urgency request (only inbound). Run `pytest tests/api/test_ask_audit.py` and confirm all pass.

- [ ] Run the full test suite with `pytest tests/ -v` from the project root. Fix any regressions. All Phase 1 and Phase 2 tests must be green. Output the final pytest summary showing passed/failed/skipped counts.
