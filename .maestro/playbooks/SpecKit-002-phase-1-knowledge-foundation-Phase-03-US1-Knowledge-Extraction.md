# Phase 03: User Story 1 — Knowledge Extraction from Chat Exports

Implement the full extraction pipeline: WhatsApp and Messenger parsers, PII stripping via spaCy NER + regex, Gemini 2.0 Flash Q&A extraction, near-duplicate detection, staging writer, import REST endpoints, and the frontend upload+polling UI. By the end of this phase a chat export can be uploaded and staged chunks are visible via GET /api/admin/kb/chunks?status=staged.

**Priority**: P1 — MVP critical. The entire downstream pipeline depends on this.

## Spec Kit Context

- **Feature:** 002-phase-1-knowledge-foundation
- **Specification:** specs/002-phase-1-knowledge-foundation/spec.md
- **Plan:** specs/002-phase-1-knowledge-foundation/plan.md
- **Research:** specs/002-phase-1-knowledge-foundation/research.md (§1 WhatsApp regex, §3 PII patterns, §4 extraction prompt)
- **Data Model:** specs/002-phase-1-knowledge-foundation/data-model.md
- **Contracts:** specs/002-phase-1-knowledge-foundation/contracts/kb-ingestion.yaml

## Tasks

### Tests (write before implementation — TDD)

- [x] T021 [US1] Write unit tests for WhatsApp parser in `backend/tests/unit/test_whatsapp_parser.py` (parametrize over DD/MM/YYYY and DD.MM.YY timestamp variants; assert multi-line message continuation is appended to previous; assert known system message strings like "Messages and calls are end-to-end encrypted" are filtered; assert empty file returns empty list)
  <!-- 21 tests collected (5 regex tests pass; 16 parse_whatsapp tests red, awaiting T026). Created: tests/unit/__init__.py, bot/__init__.py, bot/kb/__init__.py, bot/kb/parsers/__init__.py, bot/kb/parsers/whatsapp.py (stub), tests/unit/test_whatsapp_parser.py. Note: research.md regex requires dash separator; tests use Android no-bracket format + bracket+dash format accordingly. -->
- [x] T022 [P] [US1] Write unit tests for Messenger parser in `backend/tests/unit/test_messenger_parser.py` (multi-file merge deduplicates on (sender_name, timestamp_ms, content); missing `content` field is skipped; output sorted ascending by timestamp_ms; sticker/photo entries with no content are excluded)
  <!-- 18 tests collected, all red (awaiting T027). Created: bot/kb/parsers/messenger.py (stub raising NotImplementedError), tests/unit/test_messenger_parser.py. 4 test classes: TestMissingContentSkipped (4 tests), TestSortOrder (3 tests), TestMultiFileMergeAndDedup (5 tests), TestOutputShape (6 tests). Uses tmp_path fixture to write real JSON files. WhatsApp tests unchanged: 5 pass / 16 red. -->
- [x] T023 [P] [US1] Write unit tests for PII stripper in `backend/tests/unit/test_pii_stripper.py` (PERSON entity → `[IMIĘ]`; LOC entity → `[ADRES]`; phone `+48 601 234 567` → `[TELEFON]`; email `anna@gmail.com` → `[EMAIL]`; PESEL 11-digit → `[PESEL]`; post-strip re-scan flags a chunk containing a remaining 7-digit sequence)
  <!-- 9 tests collected, all RED (awaiting T028). Created: bot/kb/pii_stripper.py (stub with _nlp=None, raises NotImplementedError), tests/unit/test_pii_stripper.py. 3 test classes: TestNerReplacements (2 tests — mock _nlp to simulate PERSON→[IMIĘ] and LOC→[ADRES]), TestRegexPatterns (3 tests — phone/email/PESEL with NER mocked empty), TestPostStripRescan (4 tests — 7-digit triggers flag, 6-digit doesn't, clean text, return type). Phone-regex analysis: research.md §3 regex requires 3 groups (2-4 + 3-4 + 3-4 = min 8 digits), so bare 7-digit numbers slip through and are detected by rescan. All 5 previously-passing WhatsApp regex tests still pass. -->
- [x] T024 [P] [US1] Write unit tests for deduplicator in `backend/tests/unit/test_deduplicator.py` (same SHA-256 hash as existing chunk → `exact` flag + correct `duplicate_of_chunk_id`; mocked embedding cosine similarity 0.95 → `near` flag; mocked cosine similarity 0.85 → no flag; empty cache → no flags for any input)
  <!-- 13 tests collected, all RED (awaiting T030). Created: bot/kb/deduplicator.py (stub with DedupResult dataclass and check_duplicate/\_get_embedding raising NotImplementedError, _embedding_cache={} at module level), tests/unit/test_deduplicator.py. 4 test classes: TestExactDuplicate (4 tests — hash normalisation lower+whitespace, short-circuit no embedding call, correct chunk_id), TestNearDuplicate (3 tests — sim=0.95>0.90 threshold, chunk_id and similarity_score populated), TestBelowThreshold (3 tests — sim=0.85<0.90, flag/chunk_id/score all None), TestEmptyCache (3 tests — always flag=None, always DedupResult). Interface note: existing_hashes defined as dict[str,str] (hash→chunk_id) rather than set[str] per T030 spec, required by "correct duplicate_of_chunk_id" contract. T030 must reconcile this. All 5 previously-passing WhatsApp regex tests still pass. -->
- [x] T025 [US1] Write integration test for import flow in `backend/tests/integration/test_import_flow.py` (Firestore emulator `FIRESTORE_EMULATOR_HOST=localhost:8080`; VCR cassette for Gemini response; POST multipart to `/api/admin/kb/imports`; poll GET until `status=completed`; assert `kb_imports` doc created with correct fields; assert `kb_chunks` docs in Firestore with `status=staged` and no PII in question/answer; assert a `kb_chunk_staged` audit event was emitted for each created chunk — verify via mock GCS write capture that `event_type`, `chunk_id`, `import_id`, and `content_hash` fields are present)
  <!-- 1 test collected, RED (awaiting T026–T033). Created: tests/integration/__init__.py, tests/integration/conftest.py (vcr_config with match_on=[method,host], record_mode=none), tests/integration/test_import_flow.py (1 test: test_post_upload_and_poll_until_completed), tests/integration/cassettes/test_import_flow/test_post_upload_and_poll_until_completed.yaml (Gemini REST cassette returning 1 Q&A pair), backend/pytest.ini (registers integration mark). Core audit patched at source module (core.audit.write_event); T031 must use import core.audit; core.audit.write_event() — not from-import. existing_hashes fixture uses dict[str,str] consistent with T024. 5 WhatsApp regex tests still pass; 56 pre-existing RED tests unchanged. -->

### Implementation (in dependency order)

- [x] T026 [P] [US1] Implement WhatsApp .txt parser in `backend/bot/kb/parsers/whatsapp.py` (`WHATSAPP_LINE_RE` regex per research.md §1; iterate lines: if line matches, save previous message and start new; if no match, append to current message content; filter lines matching known system-message prefixes; return `list[dict]` with keys `timestamp`, `sender`, `content`)
  <!-- Implemented. Added _TIMESTAMP_PREFIX_RE to distinguish timestamp-prefixed system-message lines (no Sender: content) from genuine continuation lines — required to prevent system messages between real messages from being appended as continuations. All 21 tests pass (5 regex + 16 parse). -->
- [x] T027 [P] [US1] Implement Messenger .json parser in `backend/bot/kb/parsers/messenger.py` (accept list of file paths for multi-file exports; parse each `message_N.json`; skip entries without `content`; deduplicate on `(sender_name, timestamp_ms, content)` using a set; sort ascending by `timestamp_ms`; convert `timestamp_ms` to ISO-8601 UTC; return `list[dict]` with keys `timestamp`, `sender`, `content`)
  <!-- Implemented. 18/18 tests green. Dedup via set of (sender_name, timestamp_ms, content) tuples; sort key is ISO-8601 string (sorts lexicographically = chronologically for UTC). No regressions: 39 passing, 22 pre-existing RED stubs unchanged. -->
- [x] T028 [US1] Implement PII stripper in `backend/bot/kb/pii_stripper.py` (load `xx_ent_wiki_sm` once at module level; `strip_pii(text: str) -> tuple[str, bool]`: pass 1 — spaCy NER, replace `PERSON` → `[IMIĘ]`, `LOC` → `[ADRES]`, `ORG` → `[FIRMA]` using span offsets; pass 2 — four regex patterns per research.md §3 for phone/email/PESEL/NIP; post-strip rescan: flag if any digit sequence >6 digits remains; return stripped text + flag bool)
  <!-- Implemented. All 9 tests pass. Key fix: PESEL (\b\d{11}\b) and NIP patterns applied before the phone regex — the phone regex can greedily match 11-digit sequences (2-4+3-4+3-4 digit groups), so PESEL must take priority. NER spans replaced in reverse offset order to preserve positions. 48 total unit tests pass (9 PII + 21 WhatsApp + 18 Messenger), no regressions. -->
- [x] T029 [US1] Implement Gemini 2.0 Flash Q&A extractor in `backend/bot/kb/extractor.py` (extraction prompt per research.md §4; batch conversation turns into windows of 50 with 10-turn overlap when token estimate >8k; call `vertexai.generative_models.GenerativeModel(GEMINI_MODEL).generate_content()` with `response_mime_type="application/json"`; parse JSON with Pydantic `list[ChunkDraft]`; retry max 2× on `ValidationError` or `JSONDecodeError`; return `list[ChunkDraft]` — empty list is valid and signals `no_pairs_found`)
  <!-- Implemented. ChunkDraft Pydantic model with question/answer fields. _format_turns converts parsed message dicts to sender:content lines. Token estimation at 4 chars/token; windowed batching (_build_windows) only activated above 8k token threshold. _call_gemini uses vertexai.generative_models.GenerativeModel with response_mime_type="application/json". _parse_response strips markdown fences before json.loads + Pydantic TypeAdapter validation. _extract_from_turns retries up to 2× on JSONDecodeError or ValidationError, returns [] on persistent failure. extract_qa_pairs deduplicates across windows via (question, answer) key set. GEMINI_MODEL env var override supported. 48 unit tests still pass; 13 pre-existing deduplicator RED stubs unchanged. -->
- [x] T030 [US1] Implement near-duplicate detector in `backend/bot/kb/deduplicator.py` (load GCS embedding cache `gs://midwife-bot-audit-{env}/embeddings/cache.jsonl` at startup into `dict[chunk_id, embedding]`; `check_duplicate(question: str, chunk_id: str, existing_hashes: set[str]) -> DedupResult`: pass 1 — SHA-256 of normalized question (lower, collapsed whitespace); if hash in `existing_hashes` return `DedupResult(flag="exact", ...)`; pass 2 — compute Vertex AI `textembedding-gecko-multilingual@001` embedding in `eu` region; cosine similarity vs all cached embeddings; if max similarity >0.90 return `DedupResult(flag="near", similarity=..., duplicate_of_chunk_id=...)`; else return `DedupResult(flag=None)`)
  <!-- Implemented. existing_hashes typed as dict[str,str] (hash→chunk_id) per T024 contract. Pass 1 normalises with lower+collapse whitespace+SHA-256, short-circuits before any embedding call. Pass 2 calls _get_embedding (Vertex AI textembedding-gecko-multilingual@001) and computes cosine similarity via pure math.sqrt loop against _embedding_cache; skips self-comparison by chunk_id. GCS cache loaded best-effort at module import (empty on failure). 13/13 deduplicator tests pass; 61/61 unit tests pass total. -->
- [x] T031 [US1] Implement staging writer in `backend/bot/kb/staging.py` (`stage_chunks(chunks: list[ChunkDraft], import_id: str, client: AsyncClient) -> list[str]`: for each chunk: compute `content_hash = SHA-256(question + "\n" + answer)`; call `deduplicator.check_duplicate()`; create `kb_chunks` Firestore doc with fields per data-model.md (`status=staged`, `staged_at`, `source_type=export`, `import_id`, `duplicate_flag`, `duplicate_of_chunk_id`, `similarity_score`); write document to Vertex AI staging data store using Discovery Engine API with schema per data-model.md §Vertex AI Search Documents; call `audit.write_event("kb_chunk_staged", actor="midwife", import_id=import_id, chunk_id=chunk_id, content_hash=content_hash, duplicate_flag=duplicate_flag)` immediately after Firestore doc creation; return list of `chunk_id` strings)
  <!-- Implemented as async stage_chunks(). Firestore: lazy module-level singleton client. Dedup: existing_hashes built incrementally per chunk for intra-batch detection (question_hash→chunk_id dict). audit: called via core.audit.write_event (not from-import) so integration-test patch intercepts. Vertex AI write is best-effort (logs warning on failure, doesn't abort). 61/61 unit tests pass. -->
- [x] T032 [US1] Implement `POST /api/admin/kb/imports` and `GET /api/admin/kb/imports` in `backend/api/admin/kb/imports.py` (POST: validate `file` ≤10 MB and `source_format` enum; compute `filename_hash = SHA-256(original_filename)`; create `kb_imports` doc in Firestore with `status=processing`; emit `kb_import_started` audit event; enqueue `BackgroundTasks` task running: detect format → parse → PII strip → Gemini extract → dedup + stage → update `kb_imports` with `status`, `chunks_extracted`, `chunks_flagged_duplicate`, `completed_at` → emit `kb_import_completed`; return 202 `ImportCreatedResponse`; GET: list `kb_imports` newest-first with optional `status` filter and `limit` param per kb-ingestion.yaml)
  <!-- Implemented. Created backend/api/admin/kb/imports.py with POST /imports (multipart, Form source_format, file ≤10MB, SHA-256 filename_hash, Firestore kb_imports doc, audit kb_import_started, BackgroundTasks pipeline: parse→PII strip→Gemini extract→stage_chunks→update kb_imports→audit kb_import_completed; 413/400 error handling) and GET /imports (ordered by submitted_at DESC, optional status/limit filters). Registered imports_router in __init__.py. Added python-multipart>=0.0.9 to requirements.txt. 61/61 unit tests pass. -->
- [x] T033 [US1] Implement `GET /api/admin/kb/imports/{import_id}` in `backend/api/admin/kb/imports.py` (fetch `kb_imports` Firestore doc by `import_id`; return `ImportDetail` schema with `error_message` field populated on `failed` status; raise 404 if document not found)
  <!-- Added _doc_to_detail helper (extends _doc_to_summary with error_message) and GET /imports/{import_id} endpoint. Returns 404 via HTTPException when doc.exists is False. 61/61 unit tests pass; all 3 routes confirmed registered on router. -->
- [x] T034 [US1] Implement `frontend/src/pages/LoginPage.vue` (Tailwind-styled centered card; "Sign in with Google" button; on click calls `authStore.signInWithGoogle()`; on success `router.push('/kb')`; on failure display error message; show spinner during sign-in; if already authenticated redirect immediately to `/kb`)
  <!-- Implemented. Centered card via Tailwind (min-h-screen flex items-center justify-center). watchEffect redirects to /kb immediately if already authenticated. handleSignIn sets signingIn spinner, calls authStore.signInWithGoogle(), pushes /kb on success, sets errorMessage on failure. Google logo SVG inline (no external deps). -->
- [ ] T035 [US1] Create `frontend/src/services/api.ts` (typed API client: read token from `import.meta.env.VITE_ADMIN_TOKEN`; attach `X-Admin-Token` header on all requests; `createImport(file: File, sourceFormat: 'whatsapp_txt' | 'messenger_json'): Promise<ImportCreatedResponse>`; `listImports(status?: string): Promise<{imports: ImportSummary[]}>`; `getImport(id: string): Promise<ImportDetail>`; all interfaces typed per kb-ingestion.yaml schemas)
- [ ] T036 [US1] Add import upload section to `frontend/src/pages/KbReviewPage.vue` (file input accepting `.txt` and `.json`; `source_format` radio buttons; "Upload & Extract" button calling `api.createImport()`; on 202 response start polling `api.getImport(importId)` every 3 s; display current status badge (processing/completed/failed/no_pairs_found); on completed show summary: chunks extracted, duplicates flagged; stop polling on terminal status)

## Completion

- [ ] Unit tests pass: `pytest backend/tests/unit/` (T021–T024)
- [ ] Integration test passes with Firestore emulator running on :8080: `pytest backend/tests/integration/test_import_flow.py`
- [ ] POST `golden_whatsapp_export.txt` to `/api/admin/kb/imports`, poll until `status=completed`, GET `/api/admin/kb/chunks?status=staged` — confirm chunks present with PII replaced by Polish placeholders
- [ ] Verify no personal identifiers (names, phone numbers, emails) visible in any staged chunk
- [ ] Run `/speckit-analyze` to verify consistency
